---
    docker_prereqs_packages:
    - apt-transport-https
    - ca-certicates
    - curl
    - gnupg-agent
    - software-properties-common

    docker_packages:
    - docker-ce
    - docker-ce-cli
    - containerd.io  

